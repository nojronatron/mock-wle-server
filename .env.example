WINLINK_FORM_NAME=form-name.html
TEXT_TO_REPLACE="{FormServer}:{FormPort}"
FORM_SERVER_HOSTNAME="localhost"
FORM_SERVER_PORT=3001
# Protocol to use when building the form action URL. Use "http" for local/WSL/forwarded ports.
FORM_SERVER_PROTOCOL="http"

# Example: enable HTTPS and point to your key/cert (uncomment to use)
# FORM_SERVER_PROTOCOL="https"
# SSL_KEY_PATH="./server.key"
# SSL_CERT_PATH="./server.crt"